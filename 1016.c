#include <stdio.h>

int main () {

    int d, t;
    scanf("%d",&d);
    t = d * 2; // se o carro se afasta 1km a cada 2 minutos é só fazer a distancia vezes 2
    printf("%d minutos\n",t);
    return 0;
}